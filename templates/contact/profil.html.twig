{% extends 'base.html.twig' %}

{% block title %}Profil
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="text-center mt-4">Mon profil</h1>
		<h3 class="text-primary">Bonjour
			{{ utilisateur.prenom }}</h3>
		<p class="mt-3">
			<strong>Nom:</strong>
			{{ utilisateur.nom }}</p>
		<p class="mt-3">
			<strong>Prenomom:</strong>
			{{ utilisateur.prenom }}</p>
		<p>
			<strong>Téléphone:</strong>
			{{ utilisateur.telephone }}</p>
		<p>
			<strong>Email:</strong>
			{{ utilisateur.email }}</p>
		<p>
			<strong>Adresse:</strong>
			{{ utilisateur.adresse }}
			{{  utilisateur.cp }}
			{{ utilisateur.ville }}</p>
			<a href="{{ path('app_logout') }}">
  	<div class="col mt-4 ">
         <a href="{{path('app_login')}}">   <button class="btn text-info btn-lg  " type="button">Modifier mes coordonées
    </button></a> 
	</div>
		<div class="col mt-4 d-flex justify-content-end">
        <a href="{{ path('app_logout') }}">  <button type="submit" class="btn btn-danger btn-lg">Déconnexion</button> </a>

		</div>
            
	</div>
{% endblock %}
