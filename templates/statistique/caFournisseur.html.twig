{# {% extends 'base.html.twig' %} #}
{% block stylesheets %}
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
{% endblock %}


{% block body %}
	<style>

		#grapheCA{

			width: 1000px !important; /* Changer la largeur du graphe */
			height: 600px !important; /* Changer la hauteur du graphe */
		}
	</style>
	<div class="container">
		<h3 class="text mt-4">Chiffre d'affaire  par fournisseur</h3>
		<div class="row">
			<div class="col-md-8">
				<canvas id="grapheCA" class="img-fluid"></canvas>

			</div>
		</div>
		
				<form method="get" action="{{ path('caFournisseur') }}">
					<label for="annee" class="form-label mt-4">Sélectionner une année :</label>
                 <div class="row ">
			<div class="col-md-3">
					<select name="annee" class="form-select" id="annee">
                    
						{% for annee in (now|date("Y") - 10)..(now|date("Y") ) %}
							<option value="{{ annee }}" {% if annee == anneeSelectionnee %} selected {% endif %}>{{ annee }}</option>
						{% endfor %}
					</select>
				</div>

				<div class="col-md-2">
					<button type="submit" class="btn btn-primary">Afficher</button>
				</div>
			</div>
		</form>

	</div>
{% endblock %}

{% block scripts %}
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
	<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
	<script>
		

var chiffreAffaires =object.values(chiffreAffaires) ;
{{ dump() }};
var CA = document.getElementById('grapheCA');
var recetteGraphe = new Chart(CA, {
        type: 'bar',
        data: {
        labels: nomFournisseurs,
        datasets: [
       {
         label: 'Chiffre d\'affaires par fournisseur',
         data: chiffreAffaires,
         backgroundColor:  "blue",
         borderColor: 'rgba(54, 162, 235, 1)',
         borderWidth: 1
        }
      ]
    },
     options: {

      scales: {
            y: {
               beginAtZero: true
               }
              }
             }
     });
</script>
{% endblock %}
