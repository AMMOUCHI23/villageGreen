{% extends 'base.html.twig' %}

{% block title %}Village Green
{% endblock %}

{% block body %}
	<div class="container text-center text-primary my-5">
    <h1 class="text-center">Nos Catégories</h1>
    <div class="row justify-content-center">
        {% for categorie in categories %}
            <div class="col-6 col-md-3 my-4 survol">
                <a href="{{ path('Sous_categorie', {id: categorie.id}) }}"><img class="img-thumbnail rounded-circle" src="{{ asset('assets/images/' ~ categorie.photo) }}" style="height: 200px;">
                <h3 class="nomCategorie my-2">{{ categorie.nomCategorie }}</h3></a>
            </div>
        {% endfor %}
    </div>
</div>

{% endblock %}
