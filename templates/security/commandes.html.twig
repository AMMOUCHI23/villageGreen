{% extends 'base.html.twig' %}

{% block title %}Profil
{% endblock %}

{% block body %}
	<div class="container col-md-6">

		<h4 class="text mt-4">Mes Commandes</h4>
        <table class="table table-hover">
						
						</h4>
                        <a href="{{ path('accueil') }}">{{' < '}}profil</a>
						<tbody>
							{% for commande in commandes %}
								<tr>
									<td>
										<div class="card mb-3" style="max-width: 540px;">
											<div class="row g-0">
												<div class="col-md-4">
													<img src="..." class="img-fluid rounded-start" alt="...">
												</div>
												<div class="col-md-8">
													<div class="card-body">
                                                    <div class="d-flex justify-content-between">
                                                    <h5 class="card-title"> {{ commande.totalCommande }} €</h5>
                                                    <p class="card-text ">
															{{ commande.dateCommande |date('Y-m-d')}}</p>
                                                    </div>
														
														<p class="card-text ">
															{{ commande.statut }}</p>
													

                                                           
                                                            <p class="card-text">N° commande :
															{{ commande.id }}</p>
                                                            
														<a href="{{ path('detailsCommande',{'id':commande.id}) }} ">Détails{{'>'}}</a>
													</div>
												</div>
											</div>
										</div>
										

								


								</tr>
							{% endfor %}

						</tbody>
						
					</table>
		
	</div>

{% endblock %}
